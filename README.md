after cloning 

cd client : npm install && npm run dev to start
cd server : npm install && npm run dev to start backend server and cconnection with database

note : to fetch and add testimonials on home page and send data to contact form must start backend server by writting npm run dev on terminal
